# `dna search`

Alias of ``dna recall`` (neutral naming).

!!! info "Generated from the command definitions"

    This page is introspected from the `dna` Click command tree by
    `scripts/gen_cli_docs.py`, so it stays in lockstep with
    `dna search --help`.

## `dna search`

Alias of ``dna recall`` (neutral naming).

```text
dna search [OPTIONS] QUERY
```

**Arguments**

| Argument | Required |
| --- | --- |
| `QUERY` | yes |

**Options**

| Option | Description |
| --- | --- |
| `--help` | Show this message and exit. |
| `--json` |  |
| `--kind` |  |
| `--scope` |  |
| `--tenant` |  |
| `-k`, `--limit` | _(default: `10`)_ |

