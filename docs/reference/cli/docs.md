# `dna docs`

Browse the in-product Doc corpus.

!!! info "Generated from the command definitions"

    This page is introspected from the `dna` Click command tree by
    `scripts/gen_cli_docs.py`, so it stays in lockstep with
    `dna docs --help`.

## `dna docs list`

List all docs (sidebar metadata).

```text
dna docs list [OPTIONS]
```

**Options**

| Option | Description |
| --- | --- |
| `--help` | Show this message and exit. |
| `--json` |  |
| `--locale` | _(default: `pt-BR`)_ |

## `dna docs show`

Print the full markdown body of a doc.

```text
dna docs show [OPTIONS] DOC_NAME
```

**Arguments**

| Argument | Required |
| --- | --- |
| `DOC_NAME` | yes |

**Options**

| Option | Description |
| --- | --- |
| `--help` | Show this message and exit. |
| `--locale` | _(default: `pt-BR`)_ |

