# Function: validateCronExpression()

```ts
function validateCronExpression(expr): void;
```

Validate a 5-field cron expression. Throws on the first problem
(didactic message); returns void when valid.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `expr` | `string` |

## Returns

`void`
