# Type Alias: Raw

```ts
type Raw = Record<string, unknown>;
```

A raw manifest document dict (pre-`Document` parse) — the payload half
 of a [Contribution](TypeAlias.Contribution.md). Twin of the Py `Raw = dict[str, Any]` alias.
