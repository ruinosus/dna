{
  "name": "dna-sdk",
  "version": "0.6.0",
  "description": "DNA — Domain Notation of Anything (TypeScript SDK)",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ruinosus/dna.git",
    "directory": "packages/sdk-ts"
  },
  "homepage": "https://ruinosus.github.io/dna/",
  "bugs": "https://github.com/ruinosus/dna/issues",
  "keywords": [
    "dna",
    "agents",
    "declarative",
    "microkernel",
    "manifests",
    "prompts",
    "skills",
    "sdlc"
  ],
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./testing": {
      "types": "./dist/testing/index.d.ts",
      "import": "./dist/testing/index.js",
      "default": "./dist/testing/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && bun run scripts/copy-dist-assets.ts",
    "docs:api": "bun run scripts/gen-api-docs.ts"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "ajv": "^8.18.0",
    "js-sha256": "^0.11.1",
    "js-yaml": "^4.1.0",
    "mustache": "^4.2.0",
    "pg": "^8.20.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mustache": "^4.2.5",
    "bun-types": "^1.3.0",
    "sqlite-vec": "^0.1.9",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^5.5.0"
  },
  "//peerDependencies": "OPTIONAL opt-in extras, never in the default bundle: (1) rec-embedding-port — the REAL EmbeddingPort (ONNX all-MiniLM-L6-v2 via transformers.js), dynamic-imported by OnnxEmbeddingProvider only on first embed() (guard: tests/embedding-import-isolation.test.ts); (2) rec-embeddable-provider — sqlite-vec, the loadable extension for SqliteVecRecordSearchProvider, dynamic-imported by src/adapters/search/driver.ts (guard: tests/search-import-isolation.test.ts). It IS a devDependency too so `bun test` runs the search provider conformance for real, offline. Under Bun on macOS set DNA_SQLITE_LIB to a libsqlite3 that permits extension loading (e.g. Homebrew's).",
  "peerDependencies": {
    "@huggingface/transformers": "^3.0.0",
    "sqlite-vec": "^0.1.6"
  },
  "peerDependenciesMeta": {
    "@huggingface/transformers": {
      "optional": true
    },
    "sqlite-vec": {
      "optional": true
    }
  }
}
