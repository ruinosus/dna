{
  "name": "@dna/sdk",
  "version": "0.1.0",
  "description": "DNA — Domain Notation of Anything (TypeScript SDK)",
  "type": "module",
  "main": "src/index.ts",
  "files": [
    "src/**/*.ts",
    "src/extensions/*/templates/**",
    "src/extensions/*/kinds/**"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "docs:api": "bun run scripts/gen-api-docs.ts"
  },
  "dependencies": {
    "@types/pg": "^8.20.0",
    "ajv": "^8.18.0",
    "js-sha256": "^0.11.1",
    "js-yaml": "^4.1.0",
    "mustache": "^4.2.0",
    "pg": "^8.20.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/mustache": "^4.2.5",
    "bun-types": "^1.3.0",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^5.5.0"
  },
  "//peerDependencies": "rec-embedding-port — the REAL EmbeddingPort (ONNX all-MiniLM-L6-v2 via transformers.js). Optional: the default/CI floor is the zero-dep FakeEmbeddingProvider, and OnnxEmbeddingProvider dynamic-imports this only on first embed(). Never in the default bundle (guard: tests/embedding-import-isolation.test.ts).",
  "peerDependencies": {
    "@huggingface/transformers": "^3.0.0"
  },
  "peerDependenciesMeta": {
    "@huggingface/transformers": {
      "optional": true
    }
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
