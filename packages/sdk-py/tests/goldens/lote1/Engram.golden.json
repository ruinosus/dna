{
  "_doc": "FROZEN GOLDEN — captured 2026-06-10 from the live LessonLearnedKind class (pre-deletion), F3 lote-1. Deep-equaled against the descriptor-synthesized port in test_lote1_descriptor_equivalence.py. Renamed LessonLearned -> Engram (s-engram-rename, 2026-07-19): only the identity block (alias/api_version/origin/kind/display_label) changed below — schema/storage/summary_cases are byte-identical to the pre-rename capture (schema copied verbatim into helix/kinds/engram.kind.yaml).",
  "dep_filters": {},
  "flags": {
    "flatten_in_context": false,
    "is_overlayable": true,
    "is_prompt_target": false,
    "is_root": false,
    "is_runtime_artifact": false,
    "plane": "record",
    "prompt_target_priority": 0,
    "scope": null,
    "scope_inheritable": false,
    "volatile_spec_fields": [
      "created_at",
      "updated_at",
      "version"
    ]
  },
  "identity": {
    "alias": "helix-engram",
    "api_version": "github.com/ruinosus/dna/v1",
    "ascii_icon": "💭",
    "display_label": "Engrama",
    "docs": "An Engram is an affective recall artifact (record plane) — the memory co-pillar's rich, bi-temporal engram. It surfaces unbidden when the current cycle resembles a past one in the same ``area``, carries an evocative ``affect`` (triumph/regret/surprise/wistful/ominous), and is scored by Ebbinghaus-style decay (``relevance_decay_seed``, ``surface_count``, ``confidence_score``) plus Semon-inspired ecphory (``cues_history``, ``homophonic_links``). Bi-temporal — a superseded Engram is invalidated via ``valid_to``/``superseded_by_memory``, never hard-deleted. Renamed from LessonLearned (s-engram-rename, 2026-07-19) — memory is a platform primitive (``github.com/ruinosus/dna/v1``), not sdlc-owned. Authored by the Sage oracle during the deep-sleep ritual (mostly) or manually; written/recalled via ``dna.memory.remember`` / ``recall``. Stored as ``lessons-learned/<name>.yaml`` with body prose in the ``LESSON_LEARNED.md`` bundle marker — storage container/marker names are unchanged by the rename.",
    "graph_style": {
      "fill": "#FBBF24",
      "stroke": "#D97706",
      "text_color": "#fff"
    },
    "kind": "Engram",
    "origin": "github.com/ruinosus/dna/engram"
  },
  "schema": {
    "properties": {
      "affect": {
        "description": "Emotional tone. Evocative palette (Spec §15 decision): triumph, regret, surprise, wistful, ominous.",
        "enum": [
          "triumph",
          "regret",
          "surprise",
          "wistful",
          "ominous"
        ],
        "type": "string"
      },
      "affect_evidence_refs": {
        "description": "Concrete refs (rem-X, verdict-Y, Story/s-Z) that back the affect choice. Required for high-stakes affects so the LLM's claim is auditable against actual artifacts in the manifest.",
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "affect_reason": {
        "description": "Story s-remembrance-affect-reason-required. Concrete justification for the chosen affect — names specific slugs/SHAs/AC counts/state. NOT generic ('Story closed', 'shipped successfully'). Validator rejects writes that lack reason OR have boilerplate. Required for high-stakes affects (regret/ominous/surprise); optional for triumph/wistful but encouraged.",
        "minLength": 20,
        "type": "string"
      },
      "area": {
        "description": "Scoped target: Feature/X, Epic/Y, or Roadmap/Z. The LessonLearned surfaces when this area is touched.",
        "type": "string"
      },
      "confidence_score": {
        "default": 1.0,
        "description": "Semon engram intensity. Multiplies the recall score. Bumps when homophonic LessonsLearned (same area) are filed — engrams reinforce each other. Decays with surface_count for hygiene.",
        "type": "number"
      },
      "cues_history": {
        "default": [],
        "description": "Semon ecforia trace. Each time the LessonLearned is surfaced via remember(), the cue (query + actor + timestamp) is appended. History of WHY this memory kept getting recalled.",
        "items": {
          "properties": {
            "actor": {
              "type": "string"
            },
            "at": {
              "format": "date-time",
              "type": "string"
            },
            "cue": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "type": "array"
      },
      "encoding_context": {
        "additionalProperties": true,
        "description": "Snapshot of the conditions at engraphy. semon-recaller scores ecphory candidates by partial-match against this dict.",
        "properties": {
          "affect": {
            "type": "string"
          },
          "area": {
            "type": "string"
          },
          "co_topics": {
            "description": "Last 3-5 topic tags active at engraphy.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "day_of_week": {
            "type": "string"
          },
          "engraphed_by": {
            "description": "Agent slug that decided to engraph (semon-scribe by default).",
            "type": "string"
          },
          "source_refs": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "time_of_day": {
            "description": "morning|afternoon|evening|night — coarse temporal cue.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "homophonic_links": {
        "default": [],
        "description": "Semon homophony — engrams sharing substrate features. Each link records target + resonance score + basis. semon-recaller propagates a small strength boost (+0.02) to neighbors on ecphory (resonance).",
        "items": {
          "properties": {
            "basis": {
              "description": "co-area | co-affect | co-temporal | semantic | manual",
              "type": "string"
            },
            "resonance_score": {
              "maximum": 1,
              "minimum": 0,
              "type": "number"
            },
            "target_name": {
              "type": "string"
            }
          },
          "required": [
            "target_name"
          ],
          "type": "object"
        },
        "type": "array"
      },
      "last_surfaced": {
        "description": "Auto-stamped on each surfacing; null until first surface.",
        "format": "date-time",
        "type": "string"
      },
      "memory_type": {
        "type": "string",
        "minLength": 1,
        "description": "What KIND of memory this is. The three CoALA names are the ones the runtime treats specially — `episodic` (what happened), `semantic` (a generalized fact), `procedural` (how to act; the runtime presents it to the model as a RULE to follow, not as an anecdote to consider).\n⭐ OPEN ON PURPOSE, and this was a deliberate reversal. It was a closed enum of exactly those three, and a closed enum makes the vocabulary of the DOMAIN a constant of the CODE — which is the one thing this whole system exists to avoid. A workspace whose work has `preference`, `constraint` or `commitment` should be able to say so on the day it decides that, not on the day someone remembers to edit a schema.\nThe runtime honours the distinction WITHOUT knowing the name: an unrecognised type is presented to the model under its own name, because the name IS the information — `[preference]` tells a model more than `[fact]`, and far more than being dropped.\n⚠️ The consequence to accept: an unrecognised type gets no special treatment. Only `procedural` is imperative. A workspace that invents `regra_dura` gets `[regra_dura]`, not the RULE framing — because promoting an unknown name to an obligation would put words in the agent's mouth that nobody wrote.\nAbsent = untyped (legacy)."
      },
      "owner": {
        "description": "ATTRIBUTION: which agent authored this memory (e.g. claude-code, jarvis). Orthogonal to scope + to tenant (tenant separates USERS, owner separates AGENTS). Recall AUDIENCE is governed by `visibility`, NOT by owner (s-agent-memory-phase-0-bridge, 2026-06-02 — supersedes the 2026-05-17 owner-implies-private semantics). When absent: an unowned/project lesson (shared).",
        "type": "string"
      },
      "relevance_decay_seed": {
        "default": 0.95,
        "description": "Multiplicative decay factor applied per 24h. Default 0.95 (~30% relevance after 14 days).",
        "type": "number"
      },
      "revisions": {
        "default": [],
        "description": "Reconsolidation log (Nader 2000 / neo-Semon). Append-only when a recall reawakens the engram and the consumer updates the summary.",
        "items": {
          "properties": {
            "at": {
              "format": "date-time",
              "type": "string"
            },
            "by": {
              "type": "string"
            },
            "delta": {
              "type": "string"
            }
          },
          "type": "object"
        },
        "type": "array"
      },
      "source_refs": {
        "description": "Pointers to source artifacts (Narrative/X, WorkflowEvent/Y, etc.) that this memory derives from.",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "type": "array"
      },
      "summary": {
        "description": "1-2 sentence 'Lembre-se de...' — the recalled essence.",
        "maxLength": 280,
        "type": "string"
      },
      "superseded_by_memory": {
        "description": "Name of the memory that invalidated this one (not `superseded_by` — that's an ADR dep_filter token). Pairs with valid_to for point-in-time audit.",
        "type": "string"
      },
      "surface_count": {
        "default": 0,
        "description": "Increments on each surface. Damps re-surfacing via the recall scoring formula (dna/memory/decay.py).",
        "type": "integer"
      },
      "surface_when": {
        "description": "Triggers that surface this LessonLearned. Mirrors how human recall fires unbidden in context.",
        "items": {
          "enum": [
            "feature_touched",
            "cycle_open",
            "session_start",
            "oracle_consult"
          ],
          "type": "string"
        },
        "minItems": 1,
        "type": "array"
      },
      "tags": {
        "default": [],
        "items": {
          "type": "string"
        },
        "type": "array"
      },
      "valid_from": {
        "description": "World-time validity start (Zep bi-temporal). Default: created_at.",
        "format": "date-time",
        "type": "string"
      },
      "valid_to": {
        "description": "World-time validity end. Set when superseded/contradicted — the memory is INVALIDATED, never hard-deleted. Default recall excludes valid_to<now.",
        "format": "date-time",
        "type": "string"
      },
      "visibility": {
        "default": "shared",
        "description": "Recall audience (the customization axis): shared = all agents in scope recall it (cross-agent knowledge, default); private = only `owner` recalls it (an agent's raw working memory); pinned = always injected into working memory at bootstrap, bypassing recall scoring (the Letta 'memory block'); archived = retained + auditable but excluded from default recall (soft-forget). Humans audit ALL regardless of visibility (audit != recall). Phase 0 (2026-06-02).",
        "enum": [
          "shared",
          "private",
          "pinned",
          "archived"
        ],
        "type": "string"
      }
    },
    "required": [
      "area",
      "surface_when",
      "source_refs",
      "affect",
      "summary"
    ],
    "type": "object"
  },
  "storage": {
    "body_as": "text",
    "body_field": "body",
    "container": "lessons-learned",
    "marker": "LESSON_LEARNED.md",
    "pattern": "bundle"
  },
  "summary_cases": {
    "full": {
      "out": {
        "affect": "wistful",
        "area": "Feature/f-cognitive-memory",
        "source_refs": [
          "Narrative/cycle-x"
        ],
        "summary": "Lembre-se daquele ciclo onde resolvemos com Spec real.",
        "surface_when": [
          "feature_touched"
        ]
      },
      "spec": {
        "affect": "wistful",
        "area": "Feature/f-cognitive-memory",
        "source_refs": [
          "Narrative/cycle-x"
        ],
        "summary": "Lembre-se daquele ciclo onde resolvemos com Spec real.",
        "surface_when": [
          "feature_touched"
        ]
      }
    },
    "partial": {
      "out": {},
      "spec": {}
    }
  }
}
