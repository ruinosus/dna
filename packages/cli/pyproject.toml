[project]
name = "dna-cli"
version = "0.53.0"
description = "DNA command-line interface — the `dna` binary for the declarative SDLC + document CRUD (kernel-local, no service required)"
readme = "README.md"
license = "MIT"
keywords = ["dna", "cli", "agents", "declarative", "sdlc", "manifests"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
# 3.12 floor: tracks dna-sdk, which is >=3.12 since s-py312-floor (PEP 696
# TypeVar defaults come from typing_extensions on <3.13 — see
# packages/sdk-py/pyproject.toml).
requires-python = ">=3.12,<3.14"
dependencies = [
    "click>=8.1.0",
    "pyyaml>=6.0",
    "rich>=13.0",
    # Published constraint (s-publish-registries): the wheel/sdist resolve
    # dna-sdk from PyPI within the same pre-1.0 minor. In THIS repo, uv
    # replaces the registry lookup with the workspace path source below
    # ([tool.uv.sources] — editable ../sdk-py), so dev installs keep tracking
    # the sibling package live; the constraint only bites consumers.
    "dna-sdk>=0.53,<0.54",
]

[project.scripts]
dna = "dna_cli:main"

[project.optional-dependencies]
# The MCP runtime face (`dna mcp serve`) — expose the live DNA (definitions +
# SDLC + memory) over the Model Context Protocol. Built on FastMCP (the
# standalone `fastmcp` framework) for native stdio+HTTP transports and built-in
# OAuth 2.1 auth, so Phase 2 (remote + authenticated) is enable+bridge, not
# build. Optional + lazily imported (dna_cli._mcp_server imports `fastmcp` only
# inside build_server), so the base install never carries it. Guard:
# tests/test_mcp_server.py::test_base_import_never_pulls_mcp.
# The >=3.2 floor is the MCP Apps (SEP-1865) surface: `fastmcp.apps.AppConfig`
# lets `list_memories`/`recall` point the `ui://dna/memory-list` template in the
# tool DECLARATION, and `@server.resource` serves the template itself.
#
# The `apps` EXTRA raises the floor to >=3.4 and brings `prefab-ui` — the
# renderer behind the Prefab cards on `list_stories` / `sdlc_digest` /
# `list_my_kinds` (`dna_cli._mcp_cards`). It belongs to THIS extra rather than
# one of its own: a face that declares `ui://dna/prefab` on three tool
# declarations and then cannot serve the resource is worse than one that never
# declared it — the host prefetches, gets a miss, and the card becomes a broken
# promise instead of an absent one. The renderer is served BUNDLED
# (self-contained, no CDN, nothing to allow through the MCP Apps
# deny-by-default CSP), which is what the wheel's ~6.6 MB renderer asset buys:
# a deliberate, accepted cost, and the reason a `dna-cli[mcp]` image grew.
# `prefab-ui` DIRETO, e não só pelo extra-de-extra.
#
# A cadeia `fastmcp[apps]` -> `fastmcp-slim[apps]` -> `prefab-ui` é declarada em
# todos os níveis, e MESMO ASSIM o pacote não foi instalado num ambiente real
# (dna-cloud, 31/07): o resolvedor trouxe `fastmcp-slim` como dependência simples
# e o extra dele se perdeu no caminho.
#
# O efeito foi o pior tipo de falha: `review_kind` e toda tool que renderiza card
# morriam com `ModuleNotFoundError: No module named 'prefab_ui'` — no SERVIDOR,
# devolvendo "erro interno" ao agente. Quem consumia gastou três rodadas
# procurando o defeito no cliente, atrás de um dado que nunca foi produzido.
#
# Um import direto no código é uma dependência direta, mesmo quando alguma
# cadeia de extras deveria trazê-la.
#
# `mcp` DECLARADO, e não recebido por transitividade (medido em 31/07/2026).
#
# `fastmcp` não implementa o protocolo MCP: ele declara `mcp<2.0,>=1.24.0` e
# importa `mcp.types` / `mcp.server`. O `LATEST_PROTOCOL_VERSION` que rodamos
# vem do pacote `mcp`, não dele. E `_mcp_server.py` faz
# `from mcp.types import TextContent` — um import DIRETO.
#
# Import direto é dependência direta, mesmo quando alguma cadeia deveria
# trazê-la (a lição que o `prefab-ui`, três linhas abaixo, custou caro).
#
# O ganho concreto: a versão da SPEC passa a ser decisão nossa em vez de efeito
# colateral do upgrade do `fastmcp`. Medido — `fastmcp[apps]>=3.4` resolve com
# `mcp==1.29.0` sem conflito (85 pacotes), então subir a spec é bumpar este
# piso, não trocar de camada.
mcp = ["fastmcp[apps]>=3.4", "prefab-ui>=0.18.0", "mcp>=1.28"]
# The REST read-API face (`dna api serve`) — the correct HTTP boundary for a WEB
# app (the DNA Cloud portal): a normal request/response REST API over the SAME
# DNA kernel + the SAME `*_impl` cores the MCP server uses (zero logic
# duplication). Distinct from the `mcp` extra, which is for stateful AI clients.
# FastAPI (ASGI app) + uvicorn (server). Optional + lazily imported
# (dna_cli._rest_api imports `fastapi` only inside build_app), so the base
# install never carries it.
api = ["fastapi>=0.110", "uvicorn>=0.30"]
# A face A2A (Agent2Agent, governado pela Linux Foundation) — servir e consumir
# pelo SDK OFICIAL, `a2a-sdk`. Extra próprio, ao lado de `mcp` e `api`, por uma
# razão medida: a árvore BASE do a2a-sdk traz protobuf, google-api-core e
# googleapis-common-protos mesmo para quem só usa JSON-RPC (herança do
# transporte gRPC). São oito dependências, três pesadas. Quem não serve A2A não
# paga; quem serve paga o protobuf sem usar, e esse é o custo da conformidade —
# que é o produto.
#
# `[fastapi]` porque é o que traz `a2a.server.routes.fastapi_routes`
# (`add_a2a_routes_to_fastapi`) + sse-starlette, e a face servida monta num
# FastAPI que o host já tem. Sem gRPC: o `dna` fala JSON-RPC, e o extra `grpc`
# arrastaria grpcio/grpcio-tools por um transporte que não servimos.
#
# Teto `<2`: o 1.x já foi uma quebra grande (os tipos viraram protobuf, e
# `AgentInterface.transport` virou `protocolBinding`). Subir de major é decisão,
# não efeito colateral de um `pip install`.
a2a = ["a2a-sdk[fastapi]>=1.1.2,<2"]
# The Microsoft On-Behalf-Of (OBO) graph adapter — `graph.*` MCP tools that
# exchange the verified inbound Entra token for a downstream Microsoft Graph
# token and act on the signed-in user's M365 (feature f-mcp-obo, ADR-mcp-obo).
# `msal` = the confidential-client OBO exchange (acquire_token_on_behalf_of);
# `httpx` = the outbound Graph call. Optional + lazily imported (dna_cli.graph
# imports msal/httpx only inside the exchange/Graph-call seams), off by default
# (needs the `graph:` config block), HTTP-only. The base install never carries it.
graph = ["msal>=1.28", "httpx>=0.27"]
# The DURABLE quota meter — `_mcp_quota.PostgresQuotaStore`, the counter DNA
# Cloud's overage billing reads. Needs a SYNCHRONOUS Postgres driver: the
# QuotaStore port is sync (`enforce_quota` is sync), and asyncpg — the driver
# `dna-sdk[postgres]` ships — is async-only, so it cannot back this store.
# `sqlalchemy` is already implied by dna-sdk[postgres] but is named here so the
# extra stands alone. Optional + lazily imported (the store imports sqlalchemy
# only inside its own methods), so the base install and the in-process meter
# never carry it. The counter TABLE is created by the SDK's Alembic ladder
# (revision 0002_quota_counters) — nothing to provision separately.
quota = ["sqlalchemy>=2", "psycopg2-binary>=2.9"]
dev = [
    "pytest>=8.0",
    # pytest-xdist — REQUIRED, not optional: [tool.pytest.ini_options] below sets
    # `addopts = ["-n", "auto"]`, so without this a plain `pytest` dies on an
    # unrecognized argument rather than merely running serially. sdk-py has
    # carried it in [dev] for a while; the cli never did, because the cli never
    # used it. (perf/testes-em-paralelo)
    "pytest-xdist>=3.6",
    # search-sqlite extra so `dna recall`/`dna research recall` end-to-end tests
    # (tests/test_recall_cmd.py) run the real provider offline in CI, instead of
    # importorskip. Resolves against the path-installed dna-sdk.
    "dna-sdk[search-sqlite]",
    # `fastmcp[apps]` so tests/test_mcp_server.py + tests/test_mcp_apps.py +
    # tests/test_mcp_cards.py run the MCP server, the memory-card surface AND
    # the Prefab cards for real in CI — the base install keeps it optional, see
    # the `mcp` extra above. The `apps` extra is what pulls `prefab-ui`; without
    # it test_mcp_cards.py importorskips and the cards ship unproven.
    "fastmcp[apps]>=3.4",
    # `fastapi` + `httpx` so tests/test_rest_api.py drives the REST read-API for
    # real via FastAPI's TestClient (httpx-backed). The base install keeps FastAPI
    # optional — see the `api` extra above.
    "fastapi>=0.110",
    "httpx>=0.27",
    # `msal` so tests/test_mcp_graph.py can import the OBO exchanger's default
    # seam (the exchange itself is faked in tests — no live Entra). The base
    # install keeps it optional — see the `graph` extra above.
    "msal>=1.28",
    # `sqlalchemy` + `psycopg2-binary` so tests/test_mcp_quota_postgres.py can
    # drive the DURABLE quota store against a real Postgres (the tests skip
    # without a DSN — see the `quota` extra above).
    "sqlalchemy>=2",
    "psycopg2-binary>=2.9",
]

[tool.pytest.ini_options]
pythonpath = ["tests"]
# Run the suite across every core by default (perf/testes-em-paralelo) — same
# switch, same reasoning, as packages/sdk-py/pyproject.toml. This suite went
# from 483 s to ~106 s wall with an identical outcome set (1303 passed / 24
# skipped / 1 pre-existing failure, failing for the same reason), verified by
# diffing per-test-id outcomes serial-vs-parallel at two worker counts.
#
# Unlike sdk-py this suite is `call`-dominated, not `setup`-dominated (566 s in
# test bodies vs 56 s in fixtures), so parallelism is the ONLY lever here —
# there is no fixture-scope win to take.
#
# ESCAPE HATCH: `pytest -n 0` restores the single-process, in-order run.
addopts = ["-n", "auto"]
markers = [
    "requires_network: skip unless outbound network is available (tests/conftest.py; DNA_OFFLINE=1 forces the skip)",
    "requires_postgres: skip unless a Postgres DSN is set (DATABASE_URL / DNA_PG_TEST_URL / DNA_PG_TEST_DSN)",
]
filterwarnings = [
    # `POST /v1/kinds` takes a body field literally named `schema` — the WIRE
    # name, which is what a JSON Schema field must be called and is therefore
    # not ours to rename. FastAPI builds a pydantic model per body, and pydantic
    # warns because `BaseModel.schema` is a (deprecated) attribute. Silenced
    # narrowly, by the exact field and the exact generated model, so any OTHER
    # shadowed field still shows up: building the app is the first thing every
    # REST test does, so this fired once per test and drowned the real output.
    'ignore:Field name "schema" in "Body_author_kind_v1_kinds_post" shadows an attribute in parent "BaseModel":UserWarning',
    # The same wire name on the way OUT: `GET /v1/kinds/{kind}` returns the
    # authored Kind's JSON Schema under `schema`, because the request body and
    # the stored document both call it that and a third spelling for one field
    # would be a third vocabulary for one thing. Silenced by the exact model and
    # the exact field, as above.
    'ignore:Field name "schema" in "AuthoredKindDetail" shadows an attribute in parent "AuthoredKindSummary":UserWarning',
    # The third door with the same wire name: `GET /v1/kinds/registry/{kind}`
    # returns the REGISTERED Kind's JSON Schema under `schema` — same
    # reasoning, same narrow silence.
    'ignore:Field name "schema" in "RegisteredKindView" shadows an attribute in parent "BaseModel":UserWarning',
    # O eco do schema autorado na resposta da porta (o card MCP Apps o lê).
    'ignore:Field name "schema" in "AuthorKindResponse" shadows an attribute in parent "BaseModel":UserWarning',
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dna_cli"]

# Dev-workspace override: inside this repo, uv resolves dna-sdk from the
# sibling package (editable), NOT from PyPI — the registry constraint above
# is what published artifacts carry. Two paths, one name:
#   dev  (this repo)  → ../sdk-py editable, always current
#   user (pip/uv add) → PyPI dna-sdk>=0.53,<0.54
[tool.uv.sources]
dna-sdk = { path = "../sdk-py", editable = true }

[project.urls]
Homepage = "https://ruinosus.github.io/dna/"
Repository = "https://github.com/ruinosus/dna"
Documentation = "https://ruinosus.github.io/dna/reference/cli/"
Changelog = "https://github.com/ruinosus/dna/blob/main/CHANGELOG.md"
Issues = "https://github.com/ruinosus/dna/issues"
