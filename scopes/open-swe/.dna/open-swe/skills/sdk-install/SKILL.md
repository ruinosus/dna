---
name: sdk-install
description: "Install project dependencies and configure the development environment"
---

When setting up a project:
1. Detect the package manager (npm, yarn, pnpm, bun)
2. Run the appropriate install command
3. Verify the lock file is up to date
4. Check for peer dependency warnings
5. Run the project's setup script if available
