apiVersion: github.com/ruinosus/dna/sdlc/v1
kind: Plan
metadata:
  name: plan-s-prm-issuer-verbatim
spec:
  title: 'Plano: s-prm-issuer-verbatim'
  date: '2026-07-25'
  status: accepted
  story_ref: s-prm-issuer-verbatim
  journey_phase: plan
  body: 'TDD. 1) RED: a test in packages/cli/tests/test_mcp_auth.py that serves the PRM route via Starlette TestClient and asserts the raw JSON body''s authorization_servers equals the configured strings byte-for-byte (bare host, trailing-slash, and path variants). 2) GREEN: a thin RemoteAuthProvider subclass in _mcp_auth.resource_server that lets FastMCP build its routes, then wraps the PRM route''s ASGI app to rewrite only that one field with the configured strings. 3) Prove the guard bites by reverting the fix. 4) Audit resource/jwks_uri/AS-metadata for the same coercion.'
  created_at: '2026-07-25T19:11:42+00:00'
  updated_at: '2026-07-25T19:11:42+00:00'
